name: Dynamic Country EPG (WG++)
on:
  repository_dispatch:
    types: [run_webgrab]
  workflow_dispatch:
    inputs:
      country:
        description: 'Country Code (e.g., CA)'
        required: true
        default: 'CA'

jobs:
  grab-epg:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create Directories
        run: mkdir -p epg_db config/siteini.pack

      - name: Download SiteInis
        run: |
          curl -L https://github.com/SilentButeo2/webgrabplus-siteinipack/archive/refs/heads/master.tar.gz | tar xz --strip-components=1 -C config/siteini.pack

      - name: Run Python Filter
        run: |
          COUNTRY=${{ github.event.client_payload.country || github.event.inputs.country }}
          python3 filter_wg_channels.py $COUNTRY
          echo "COUNTRY=$COUNTRY" >> $GITHUB_ENV

      - name: Run WebGrab+Plus (Direct .NET Mode)
        run: |
          # This command points specifically to the app's DLL inside the container
          docker run --rm \
            -v $(pwd)/config:/config \
            -v $(pwd)/epg_db:/data \
            -e PUID=1000 -e PGID=1000 \
            lscr.io/linuxserver/webgrabplus:latest \
            dotnet /app/wg++/WebGrab+Plus.dll /config

      - name: Update Status to Ready
        run: |
          # Creates status.json so your playlist generator knows the file is live
          python3 -c "import json, datetime; d={'country':'${{ env.COUNTRY }}', 'status':'ready', 'last_run': str(datetime.datetime.now())}; json.dump(d, open('epg_db/status.json', 'w'))"

      - name: Commit and Push
        run: |
          git config --global user.name "EPG-Bot"
          git config --global user.email "bot@github.com"
          git add epg_db/
          git commit -m "Updated EPG for ${{ env.COUNTRY }} [skip ci]" || exit 0
          git push
