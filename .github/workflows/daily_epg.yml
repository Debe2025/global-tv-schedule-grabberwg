name: Dynamic Country EPG (WG++)
on:
  repository_dispatch:
    types: [run_webgrab]
  workflow_dispatch:
    inputs:
      country:
        description: 'Country Code (e.g., CA)'
        required: true
        default: 'CA'

jobs:
  grab-epg:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Create Directories
        run: mkdir -p epg_db config/siteini.pack

      - name: Download SiteInis
        run: |
          curl -L https://github.com/SilentButeo2/webgrabplus-siteinipack/archive/refs/heads/master.tar.gz | tar xz --strip-components=1 -C config/siteini.pack

      - name: Run Python Filter
        run: |
          COUNTRY=${{ github.event.client_payload.country || github.event.inputs.country }}
          python3 filter_wg_channels.py $COUNTRY
          echo "COUNTRY=$COUNTRY" >> $GITHUB_ENV

      - name: Run WebGrab+Plus (Network Host Mode)
        run: |
          docker run --rm \
            --network="host" \
            -v $(pwd)/config:/config \
            -v $(pwd)/epg_db:/data \
            -e PUID=1000 -e PGID=1000 \
            lscr.io/linuxserver/webgrabplus:latest \
            /bin/sh -c "
              DOTNET_BIN=\$(find / -name dotnet -type f -executable | head -n 1)
              MONO_BIN=\$(find / -name mono -type f -executable | head -n 1)
              if [ -n \"\$DOTNET_BIN\" ]; then
                DLL_PATH=\$(find /app -name WebGrab+Plus.dll | head -n 1)
                \$DOTNET_BIN \$DLL_PATH /config
              elif [ -n \"\$MONO_BIN\" ]; then
                EXE_PATH=\$(find /app -name WebGrab+Plus.exe | head -n 1)
                \$MONO_BIN \$EXE_PATH /config
              else
                exit 1
              fi
            "

      - name: Fix Permissions
        if: always()
        run: sudo chown -R $USER:$USER epg_db/ config/

      - name: Print WebGrab Log
        run: cat config/WebGrab++.log.txt || echo "Log file not found"

      - name: Validate Output and Update Status
        if: always()
        run: |
          python3 -c "
          import json, datetime, os
          country = '${{ env.COUNTRY }}'
          xml_exists = os.path.exists(f'epg_db/{country}.xml')
          status = 'ready' if xml_exists else 'failed'
          d = {
              'country': country,
              'status': status,
              'last_run': str(datetime.datetime.now()),
              'file_found': xml_exists
          }
          with open('epg_db/status.json', 'w') as f:
              json.dump(d, f)
          if not xml_exists:
              print(f'ERROR: {country}.xml was not created!')
          "

      - name: Commit and Push
        if: success()
        run: |
          git config --global user.name "EPG-Bot"
          git config --global user.email "bot@github.com"
          git add epg_db/
          git commit -m "Updated EPG for ${{ env.COUNTRY }} [skip ci]" || exit 0
          git push
